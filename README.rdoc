== Akeem's Rooster Grin Homework

#Initialization
bundle install
rake db:create
rake db:migrate
rake db:seed
rails s

#Tech
RoR, HTML5, CSS, JavaScript, jQuery, MaterializeCss

#Potential Future Improvements
* Error handling in NotesController, rather than front end error handling currently in place
* Underline of input color is wrong
* General UI/UX improvements
* Test Suite
* Persist notes to db
